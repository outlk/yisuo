<!--index.wxml-->
<view class="body">
  <view class="tip" hidden='{{hidetip}}'>
  <text style="font-size: 70rpx">伊索寓言v1.2</text>
  <text style="font-size: 30rpx">by 桩白墨</text>

  <view style="margin-top: 100rpx; font-size: 90rpx">\n左右滑动^_^</view>
  </view>

  <view hidden='{{!hidetip}}'>

    <view class="container1" wx:if="{{card == 1}}" bindtouchstart="touchStart" bindtouchmove="touchMove" bindtouchend="touchEnd" animation="{{ani1}}">
    <!-- container1 -->
      <view class="title">{{buffer[page].title}}</view>
      <view class="content">{{buffer[page].content}}</view>
      <view class="conclusion">{{buffer[page].conclusion}}</view>
    </view>

    <view class="container2" wx:if="{{card == 2}}"  bindtouchstart="touchStart" bindtouchmove="touchMove" bindtouchend="touchEnd" animation="{{ani2}}">
      <view class="title">{{buffer[page].title}}</view>
      <view class="content">{{buffer[page].content}}</view>
      <view class="conclusion">{{buffer[page].conclusion}}</view>
    </view>

  </view>
</view>
